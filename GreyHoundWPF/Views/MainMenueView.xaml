<UserControl x:Class="GreyHoundWPF.Views.MainMenueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GreyHoundWPF.Views"
             xmlns:commands="clr-namespace:GreyHoundWPF.Commands"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="AliceBlue">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="0.5*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Welcome to Grey Hound" FontSize="36" HorizontalAlignment="Center" Margin="0,100,0,0"/>
        <Button Grid.Row="1" Content="Start"  FontSize="28" Width="200" Height="50" Command="{Binding updateViewcommand}" CommandParameter="gamePlay"/>
    </Grid>
</UserControl>
